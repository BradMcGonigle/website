{
  "name": "utils.core.format",
  "version": "0.0.0",
  "private": true,
  "description": "Formatting utilities for currency, dates, and numbers",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo node_modules"
  },
  "devDependencies": {
    "configs.eslint": "workspace:*",
    "configs.typescript": "workspace:*",
    "configs.vitest": "workspace:*",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
